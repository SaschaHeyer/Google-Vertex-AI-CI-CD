steps:
  # Install dependencies
  - name: python
    entrypoint: pip
    args: ["install", "-r", "pipelines/requirements.txt", "--user"]

  # Compile pipeline
  - name: 'python'
    entrypoint: 'python'
    args: ['pipelines/pipeline.py']
    id: 'Compile Pipeline'